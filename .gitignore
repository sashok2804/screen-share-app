node_modules/
dist/
coverage/
*.log
.env
.env.local
.vite/

# TypeScript incremental build artifacts (from tsc -b)
*.tsbuildinfo
**/*.tsbuildinfo
vite.config.d.ts
vite.config.js
vitest.config.d.ts
vitest.config.js

# TLS certs (generated locally, never commit)
server/certs/*.pem

# Electron desktop client (Phase 1+)
electron/node_modules/
electron/dist/
electron/out/
# Downloaded FFmpeg binary — ~97MB, fetched by `npm -w electron run download-ffmpeg`.
electron/bin/
# `electron/build/` is buildResources — the icon.ico there MUST be committed,
# but we keep other generated artifacts ignored.
electron/build/*.unpacked
electron/build/*.zip
